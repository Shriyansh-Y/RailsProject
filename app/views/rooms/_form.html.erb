<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_number %><br>
    <%= f.text_field :room_number %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.select :capacity, ['small-4 chairs','medium-6 chairs','large-12 chairs']%>
  </div>
  <div class="field">
    <%= f.label :building %><br>
    <%= f.select :building, ['D.H. Hill','Hunt'] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
